# Evidence (local by default)

Raw proof artifacts live here (jsonl runs, screenshots, large outputs).
Policy: keep local unless explicitly promoted into docs/ or summarized into a small rollup file.
